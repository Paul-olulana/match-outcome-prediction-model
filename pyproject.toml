[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fa_scripts"
version = "0.1.0"
dependencies = [
    "pandas>=1.3.0",
    "numpy>=1.21.0",
]

[tool.setuptools.packages.find]
where = ["."]